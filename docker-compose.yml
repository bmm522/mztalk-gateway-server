version: "3.7"

services:
  mztalk-mysql:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_DATABASE: gateway
      MYSQL_ROOT_PASSWORD: dlquf!2tkfkd!2
      MYSQL_USER: gateway
      MYSQL_PASSWORD: 1234
#       MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
    ports:
      - 3307:3306
    networks:
      - mztalk-mysql-net
  test-app:
    build:
      context: ./
      dockerfile: Dockerfile
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mztalk-mysql:3306/gateway?autoReconnect=true&useSSL=false
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: dlquf!2tkfkd!2
    ports:
      - 8000:8000
    depends_on:
      - mztalk-mysql
    networks:
      - mztalk-mysql-net

networks:
  mztalk-mysql-net:
         driver: bridge
